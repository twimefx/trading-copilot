# Kronos GPU service deps. torch is intentionally OMITTED — the CUDA base image
# (runpod/pytorch) ships a torch wheel matched to its CUDA runtime. Installing a
# second torch here would bloat the image and can break CUDA compatibility.
fastapi~=0.138.0
uvicorn[standard]~=0.49.0
pandas~=2.2.2
numpy~=2.5.0
pydantic~=2.13.4
safetensors~=0.6.2
huggingface-hub~=0.33.1
einops~=0.8.2
runpod~=1.7.0
